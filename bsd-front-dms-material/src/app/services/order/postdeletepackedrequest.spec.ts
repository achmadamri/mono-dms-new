import { PostDeletePackedRequest } from './postdeletepackedrequest';

describe('PostDeletePackedRequest', () => {
  it('should create an instance', () => {
    expect(new PostDeletePackedRequest()).toBeTruthy();
  });
});
