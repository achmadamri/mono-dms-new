import { PostPackingResponse } from './postpackingresponse';

describe('PostPackingResponse', () => {
  it('should create an instance', () => {
    expect(new PostPackingResponse()).toBeTruthy();
  });
});
