import { PostUploadProductResponse } from './postuploadproductresponse';

describe('PostUploadProductResponse', () => {
  it('should create an instance', () => {
    expect(new PostUploadProductResponse()).toBeTruthy();
  });
});
