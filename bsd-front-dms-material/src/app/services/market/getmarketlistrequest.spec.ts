import { GetMarketListRequest } from './getmarketlistrequest';

describe('GetMarketListRequest', () => {
  it('should create an instance', () => {
    expect(new GetMarketListRequest()).toBeTruthy();
  });
});
