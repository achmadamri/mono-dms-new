import { GetSkuProductResponse } from './getskuproductresponse';

describe('GetSkuProductResponse', () => {
  it('should create an instance', () => {
    expect(new GetSkuProductResponse()).toBeTruthy();
  });
});
