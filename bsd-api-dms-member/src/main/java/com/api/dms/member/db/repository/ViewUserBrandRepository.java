package com.api.dms.member.db.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.api.dms.member.db.entity.ViewUserBrand;

public interface ViewUserBrandRepository extends JpaRepository<ViewUserBrand, Integer> {
}